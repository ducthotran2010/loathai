import styled from 'styled-components';
import Modal from '@material-ui/core/Modal';

export default styled(Modal)`
    background: #fff;
`;

export { default as ModalContainer } from './ModalContainer';
export { default as ModalExit } from './ModalExit';
